# Fidelity Live-Shape Mismatch Ledger

All live receipts are redacted or structural. Raw Fidelity exports remain
gitignored and were not copied into tracked files.

| ID | Severity | Synthetic claim | Live reality | Downstream impact | Recommended fix |
|---|---|---|---|---|---|
| FID-LS-001 | `block` | The app has investment tables for holdings/activity, and synthetic Fidelity writes `positions_ledger`, `investment_holdings`, and `portfolio_snapshots` (`scripts/dummy_data/generator.py:1204`, `:1275`, `:1284`). | Current live ingest parses/reconstructs data but `persist_to_db` records only the SPAXX cash balance via `record_balance` (`scripts/ingest_fidelity_history.py:598-636`); connector `_run_ingest` calls that same path (`extractors/fidelity_connector.py:499-517`). | Live Fidelity holdings, activity, performance, cost basis, lots, dividends, and transfer flows do not reach the tables read by Investments. | Add a Fidelity live writer that persists `positions_ledger`, `investment_holdings`, and `portfolio_snapshots` from the CSV parse before using the app for live trust. |
| FID-LS-002 | `gap` | Synthetic Fidelity emits a fixed allocation and only `INITIAL_BASELINE`/`BUY` rows at a fixed price (`scripts/dummy_data/generator.py:1078-1085`, `:1131-1198`). | Redacted history fixtures cover `YOU BOUGHT`, `REINVESTMENT`, `DIVIDEND RECEIVED`, `CAP GAIN`, EFT in/out, and `EXPIRED`. Current classifier maps most observed verbs (`scripts/ingest_fidelity_history.py:70-87`) but tests do not cover the full live verb surface. | Synthetic tests understate live action diversity; unobserved verbs may silently fall to `OTHER`. | Add parser/action tests around the live verb contract and require explicit handling for new Fidelity action families. |
| FID-LS-003 | `gap` | Synthetic Fidelity snapshots use no brokerage cash balance in the canonical allocation (`scripts/dummy_data/generator.py:1275-1282`). | Live positions include SPAXX as a money-market sweep with blank cost-basis fields; history includes SPAXX dividend and reinvestment pairs. | UI/account totals can misrepresent the equity-vs-cash split if SPAXX is treated as a normal equity holding or omitted. | Keep SPAXX/FDRXX as cash equivalents in holdings, allocation, performance, and tax-lot views. |
| FID-LS-004 | `block` | Synthetic Fidelity cash contributions are checking debits described as `FIDELITY EFT TRANSFER` and linked to one primary ledger row (`scripts/dummy_data/generator.py:1083`, `:1241-1247`; `tests/test_trusted_seed.py:106-120`). | Live history action is `Electronic Funds Transfer Received (Cash)` or `Electronic Funds Transfer Paid (Cash)`. Current live parser classifies these as `DEPOSIT`/`WITHDRAWAL`, but no bank-side transaction or `bank_txn_id` link is created. | Accountability and cash-flow reports miss or misclassify real Fidelity contributions/withdrawals. Shape B reporting requires `positions_ledger.bank_txn_id` (`dal/reports/flow.py:358-439`). | Implement live EFT matching/linking against imported bank transactions and stamp one primary ledger row per cash leg. |
| FID-LS-005 | `block` | Synthetic dividend flows depend on cash transactions categorized exactly as `Investment Income` (AI-016: `docs/data-lineage/ACTION_ITEMS.md:192-204`; flow matcher: `dal/reports/flow.py:557-620`). | Live parser classifies `DIVIDEND RECEIVED` and `CAP GAIN` as `DIVIDEND`, but it does not emit a transaction row or category. The contract gauntlet marks this with an expected xfail. | Live dividends and reinvestments disappear from income/reinvestment flows. | Write dividend/capital-gain cash transactions as `Investment Income` and pair reinvestments to `positions_ledger`. |
| FID-LS-006 | resolved (P17-T30) | Cost basis is available to holdings/lots through `investment_holdings.cost_basis` and `positions_ledger.cost_basis_dec` (`dal/investments.py:56-94`, `:193-272`). | Positions CSV `Cost Basis Total` is now persisted per-position to `investment_holdings.cost_basis` via `dal/fidelity_investment_writes.py::write_fidelity_investment_state`; `Average Cost Basis × Quantity` is the blank-cell fallback. The aggregate write to `loan_details.cost_basis` was retired; SPAXX/FDRXX rows do not receive basis. `positions_ledger.cost_basis_dec` remains evidence-based pending lot-source authority (P17-T32). | Investments UI now reads canonical Fidelity per-position basis. | Done. |
| FID-LS-007 | decided (P17-T32) | `get_lots` can display FIFO lots from ledger buy/reinvestment rows (`dal/investments.py:193-272`). | Activity plus Positions CSVs do not expose open-lot acquisition dates, lot-specific basis, or disposal selections. | Live tax-lot UI would be inferred rather than evidenced. | **Decided.** Closed-lot source: `Closed_Positions_<year>.csv` (cents-perfect against 1099-B for the 2023 sample). Open-lot per-position basis: `Portfolio_Positions` CSV (already wired by P17-T30). Per-lot open acquisition date for legacy lots remains unsolved by current downloads (in-page lot detail or statement parsing is the future option). Implementation prompt skeleton in [`tax-lot-source-recommendation.md`](tax-lot-source-recommendation.md) §4. |
| FID-LS-008 | `cosmetic` | Synthetic share deltas are quantized to five decimals (`scripts/dummy_data/generator.py:1166-1177`). | Live history samples include integer and fractional quantities with up to three decimal places in current exports; tests preserve representative fractional values through the parser. | No current precision failure observed, but the contract should prevent future truncation. | Keep decimal/string preservation tests and avoid narrowing precision in future writers. |
| FID-LS-009 | `gap` | Synthetic ledger rows populate next-business-day `settlement_date` for all generated buy/baseline rows (`scripts/dummy_data/generator.py:1182`). | Live history has blank settlement dates for cash/dividend/reinvestment-like rows and populated settlement dates for buys. Current live reconstruction reads but does not persist settlement dates. | Settlement-sensitive lot/wash-sale logic would be incomplete for live Fidelity. | Persist settlement date when present and allow blanks for cash-only rows. |
| FID-LS-010 | `cosmetic` | No strong synthetic claim; test fixtures are clean table rows. | Live history has two blank pre-header lines and footer/disclaimer rows. Current parser handles this by finding `Run Date` and dropping invalid dates (`scripts/ingest_fidelity_history.py:90-118`). | Covered today; regression risk if the parser is replaced with simple `pd.read_csv(filepath)`. | Keep header/footer fixture tests as a contract guard. |
| FID-LS-011 | resolved (P17-T30) | Synthetic amounts are simple decimal values. | `_clean_number` (`scripts/ingest_fidelity_history.py`) now parses parenthesized negatives, blank cells, trailing-space currency, and double-quote-wrapped values; `Total Gain/Loss Dollar = ($25.00)` round-trips as `-25.00` and SPAXX/FDRXX blank basis cells parse as 0.0 (then ignored downstream by the cash-equivalents check). Coverage: `tests/test_fidelity_live_shape_contract.py::test_clean_number_covers_fidelity_money_format_edge_cases`. | Done. | Done. |
| FID-LS-012 | `gap` | Synthetic Fidelity assumes one account id, `fidelity_brokerage` (`scripts/dummy_data/generator.py:1031`). | Positions CSV includes `Account Number`; current parser does not scope rows by account number (`scripts/ingest_fidelity_history.py:150-169`), and history CSV lacks an account column in the observed export. | Multiple Fidelity accounts in one export could be merged into one app account. | Add account-number scoping/redaction mapping for positions and require one-account-per-history-export or a separate account source. |
| FID-LS-013 | `gap` | Synthetic never sells; sell handling exists in `get_lots` and the live classifier but is not exercised by the seed (`dal/investments.py:224-256`; `scripts/ingest_fidelity_history.py:77`). | Current live samples have no `YOU SOLD` rows. | Sold-out lots, realized gains, and closed-position behavior remain unproved. | Capture a redacted SELL/closed-position sample before claiming tax-lot readiness. |
| FID-LS-014 | decided (P17-T32) | Synthetic dividend category is generic `Investment Income`. | Live samples include `CAP GAIN`; current classifier maps it to `DIVIDEND` (`scripts/ingest_fidelity_history.py:79-80`) with no subtype. P17-T29 writer preserves the verbatim Fidelity action verb in `transactions.raw_description` and a normalized `<TICKER> <SUBTYPE>` prefix in `transactions.description`. | Tax reporting can lose qualified dividend, short-term gain, and long-term gain distinctions if a consumer queries by subtype without consulting `description`/`raw_description`. | **Decided. No new schema.** Subtype is preserved as evidence in `transactions.description` (normalized prefix) and `transactions.raw_description` (verbatim). Writer's `_ACTION_MAP` (`dal/fidelity_dividend_income.py:36`) produces deterministic discrimination at write time. No current consumer queries by subtype; reconciliation against the 2024 1099 confirms the IRS routing rule (short-term cap gain → Box 1a; long-term → Box 2a) is recoverable from a description-prefix LIKE clause. Reversibility note in [`tax-lot-source-recommendation.md`](tax-lot-source-recommendation.md) §5.5 if a future consumer demands denormalization. |
| FID-LS-015 | decided (P17-T32) | Yearly tax document surfaces include Fidelity 1099 expectations (`dal/yearly_wrapup.py:577-601`). | The CSV live-shape audit did not validate `dal/parsers/fidelity_1099.py`; that parser is a separate PDF path. | 1099 reconciliation cannot yet be used as evidence that CSV dividends/cost basis agree with tax forms. | **Decided.** Parser run on real 2023 + 2024 Consolidated 1099 PDFs: boxes 1a/1b/2a/1 extract reliably and reconcile against History CSV `DIVIDEND RECEIVED` + `SHORT-TERM CAP GAIN` + `LONG-TERM CAP GAIN` row sums (combined 1a+2a vs combined CSV: $140.46 = $140.46 cents-perfect for tax year 2024). 1099-B Total Proceeds / Total Cost Basis / Realized Gain/Loss are silently dropped by the current regex; tracked as new follow-up `FID-LS-016`. See [`1099-reconciliation.md`](1099-reconciliation.md). |
| FID-LS-016 | `gap` | The 1099 parser's `can_commit` guard claims to require core fields including `total_proceeds` (`dal/parsers/fidelity_1099.py:73-85`). | Parser regex for `Total proceeds`, `Total cost basis`, and `Net gain (loss)` does not match the actual Fidelity 1099-B summary layout (labels are wrapped across two header rows; totals appear as an unlabeled trailing line). All three fields silently return None on real PDFs; the `can_commit` guard does NOT trip because boxes 1a/1b/2a/1 extract. | `dal/yearly_wrapup.py:577-601` reads `total_proceeds` and `total_cost_basis` into the Fidelity yearly investment-income block; both keys return None on every real-1099 input today, with no warning surfaced. | Add a tabular extractor for the `Summary of <YYYY> Proceeds From Broker and Barter Exchange Transactions` block (anchor on the per-section term-and-basis rows or on the unlabeled trailing total line); replace the `Net gain (loss)` regex with `Realized\s+Gain/Loss`; tighten `can_commit` so "section recognized but totals not extracted" surfaces as a warning. Add a redacted regression fixture under `tests/fixtures/fidelity/`. Verification target: 2023 PDF extracts `total_proceeds=5839.11`, `total_cost_basis=2759.50`, `total_gain_loss=3079.61`. See [`1099-reconciliation.md`](1099-reconciliation.md) §3 and §4.1. |
| FID-LS-017 | `gap` | The recommended Closed-Positions-as-truth path is asserted to remain sufficient for non-zero wash-sale activity. | Household's 2023 1099-B reports $0.00 wash sales disallowed across all sections; the cents-perfect Closed Positions vs 1099-B agreement is not yet stress-tested for a wash-active year. | If a future tax year carries non-zero wash adjustments and Closed Positions does not surface them, the closed-lot writer downstream of P17-T32's recommendation could miss disallowed-loss state. | When a tax year with non-zero wash sales becomes available, re-run the Closed Positions vs 1099-B reconciliation including the wash-sales-disallowed column. Confirm the recommendation still holds; otherwise, add a 1099-B wash-adjustment supplement to the closed-lot ingest path. See [`1099-reconciliation.md`](1099-reconciliation.md) §4.2. |
